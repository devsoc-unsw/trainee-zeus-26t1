fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0
# 2.3.x gotrue passes proxy= into httpx (removed); 2.15+ aligns with httpx 0.27.x.
supabase==2.15.1
# Starlette 0.27 TestClient needs httpx<0.28 (0.28 drops ASGI `app=` shortcut used by TestClient).
httpx==0.27.2
pytest==9.0.3
